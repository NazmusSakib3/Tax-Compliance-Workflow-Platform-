{
  "name": "tax-compliance-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "capture-readme-screenshots": "playwright test capture-readme-screenshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
